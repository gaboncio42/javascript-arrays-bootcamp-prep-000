var chocolateBars = ["snickers", "hundred grand", "kitkat", "skittles"];

function addElementToBeginningOfArray (array, element) {
  var beginning = [1];
  return beginning.unshift['foo'];
}

function destructivelyAddElementToBeginningOfArray (array, elemet) {
  var beginning = [1];
  return ('foo',...beginning);
}